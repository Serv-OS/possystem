// Root build.gradle. v5.5.58: Kotlin plugin removed alongside Stripe Terminal SDK ripout.
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

<?xml version="1.0" encoding="utf-8"?>
<!-- Shared by every app built on :webshell. Each app adds its own activity entry (orientation,
     launcher) and, for Staff only, the location permissions. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- In-app self-update: download + launch the system installer for a new APK. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application>
        <!-- Shares the downloaded update APK with the system installer via a content:// URI -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/webshell_file_paths"/>
        </provider>
    </application>
</manifest>
